datasource db{
    provider = "sqlite"
    url = env("DATABASE_URL")
}

generator client {
    provider = "prisma-client"
    output = "client"
    runtime = "demo"
}

model Airport{
    id        String   @id @default(cuid())
    name      String
    city      String
}